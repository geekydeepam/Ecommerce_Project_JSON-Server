{
  "name": "my-ecommerce-app",
  "version": "1.0.0",
  "description": "My eCommerce app with JSON Server",
  "main": "index.html",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
